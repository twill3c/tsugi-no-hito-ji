# HARNESS_CHANGELOG.md — ハーネス改訂台帳(tsugi-no-hito-ji)

原則: **エージェントがミスをするたびに、そのミスが二度と起きないようハーネスを改良する。**
起票条件: 同一失敗コード累計 2 回(LL-10)、または severity S1(LL-12)。

---

## HC-001

| 項目 | 内容 |
|---|---|
| 起票日 | 2026-08-29 |
| トリガー | `GEN-LOGIC` × 2(loop_001)。1 件目は docstring の英単語にキリル文字が混入(フリート共通の HC-037 と同型)。2 件目は混合係数 λ=n/(n+β) を既定 β=8 で採用したところ、**検証本文のビット数が文脈長 3 以上で単調に悪化**した(4 コーパスとも最良が L=2)。このアプリの目玉は「長い文脈ほど次の字が当たる」であり、実測はその逆を示していた |
| 診断 | 2 件目が本質。**目玉が主張する向きを、既定パラメータで一度も測らないまま SPEC に書こうとしていた。** 式そのものは教科書に載っている形で、構文としても数学としても誤りではない。誤っていたのは「この式とこの既定値で、主張したい単調性が出るか」を確かめる工程が無かったことである。図・文言の嘘は HC-045 / HC-001(wakachi-ki)で既に警戒されているが、**主張の向きそのものが実装で成り立たない**型はまだ規範になっていない。3 分の測定で分かることを、SPEC に書いてから発見していたら SPEC-DRIFT になっていた |
| 改訂 | TEST_SPEC の「オラクルの出所」に**主張の向きの節**を新設し、次を本プロジェクトの規範とする: **アプリの目玉が「増えるほど良くなる/減る/近づく」という向きを主張するなら、SPEC にその主張を書く前に、出荷する既定パラメータでその向きを実測する。** 実測が主張と逆を向いたら、まず主張ではなく実装(式・既定値)を疑い、直せないなら**主張のほうを捨てる**。向きは T-041(単調性ゲート)としてテストに残し、パラメータを触るたびに再判定する |
| 種別 | test_spec |
| SCAFFOLD_VERSION | 変更なし(プロジェクト局所。ただし「目玉の主張を実装で先に測る」は他の教材系アプリにも効くので、再発すればレジストリ還流を検討する) |
| 効果検証 | 以後 5 ループで「主張と実測が逆を向く」型の `GEN-LOGIC` 再発 0 件なら Closed |
| 状態 | Verifying |

---

## HC-002

| 項目 | 内容 |
|---|---|
| 起票日 | 2026-08-29 |
| トリガー | `VERIF-FALSE` × 2(loop_001)。1 件目は T-010 の手勘定の期待値が誤り(小さな本文の末尾にしか無い並びに「次の字がある」と書いた)。2 件目は文字種検査の**陽性対照を原稿にそのまま書いた**ため、原稿を走査する T-072 が自分の対照を撃った |
| 診断 | 2 件目は chikuma-seiki loop_007 と同型で、フリートで 2 例目。**検出系のテストには陽性対照を置け(HC-041)**という規範と、**原稿全体を走査せよ**という規範が、素直に書くと衝突する。逃げ道として「この行は見逃す」印を付けると、今度は検査に穴が開き、緩めたときに気づけなくなる。1 件目と併せると、両方とも「**書いた文字列を、実物で一度も確かめずにテストの真実として据えた**」型である |
| 改訂 | TEST_SPEC に次の 2 つを本プロジェクトの規範として置く。(1) **検出系テストの陽性対照は、悪い例を原稿に直接書かず、符号位置から実行時に組み立てる。** 検査器が受け取るのは本物の悪い例で、原稿に残るのは 16 進の数字だけになる。除外印は使わない。(2) **手勘定の期待値も、書いたら必ず一度は実物で数え直す。** 小さなフィクスチャは「読めば分かる」ので確かめずに書きがちだが、読めることと数えたことは別である |
| 種別 | test_spec |
| SCAFFOLD_VERSION | 変更なし(プロジェクト局所。(1) はフリートで 2 例目なので、3 例目が出たらレジストリ還流を提案する) |
| 効果検証 | 以後 5 ループで「対照が自分の検査に撃たれる」型と「未確認の手勘定期待値」型の `VERIF-FALSE` 再発 0 件なら Closed |
| 状態 | Verifying |

---

## HC-003

| 項目 | 内容 |
|---|---|
| 起票日 | 2026-08-29 |
| トリガー | `GEN-LOGIC` 3 回目(loop_001・stage 5 の実ブラウザ検品)。かなあて勝負の結果欄で、**人間のエントロピー下界 6.02 bit が上界 3.42 bit を上回っていた**。シャノンの下界の式は「予測器が確率の高い順に当てにいく」ことを前提にしており、人間の推測順はその前提を満たさない |
| 診断 | 式そのものは正しく、T-050 では無作為 2,000 分布で下界 ≤ H ≤ 上界を確かめてあった。**しかしその検証はすべて q が降順の場合(理想予測器)で、実際に式を当てた相手は降順でない人間だった。** 定理を検算したのに、検算した場所と使った場所で前提が違っていた。HC-001(主張の向きを先に測る)も HC-045(オラクルの循環を疑う)も、この型は捕まえない。**検算が通ったことは、その式を使ってよい相手の範囲までは教えてくれない。** なお上界のほうは「推測回数の列から本文を復元できる」ことだけから出るので予測器の巧拙によらず成り立ち、人間にも出してよい |
| 改訂 | TEST_SPEC の「オラクルの出所」に次を加える: **外部の定理・公式を使うときは、(1) 成り立つ条件を式と一緒に書き、(2) その条件を検査する述語を実装に置き、(3) 条件を満たさない入力で「実際に壊れる」ことを陽性対照として持つ。** 番人を置くだけでは足りない —— 条件を外したときに何も起きないなら、その番人は要らないか、番人の見ている条件が違う。本件では T-055 で「前提を外れた q では下界が上界を上回る」ことを主張し、T-056 で「上界は並べ替え不変(だから誰にでも使える)」ことを主張した |
| 種別 | test_spec |
| SCAFFOLD_VERSION | 変更なし(プロジェクト局所。ただし「定理の前提を述語にして陽性対照を持つ」は数理オラクルを背骨にする全アプリに効くので、2 例目が出たらレジストリ還流を提案する) |
| 効果検証 | 以後 5 ループで「定理を前提の外で使う」型の `GEN-LOGIC` 再発 0 件なら Closed |
| 状態 | Verifying |
