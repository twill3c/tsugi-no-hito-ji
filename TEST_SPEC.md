# TEST_SPEC.md — tsugi-no-hito-ji

<!-- scaffold template v1.13.0 から展開(2026-08-29) -->

## 実行規約

- `python -m pytest -x -q` を stage 3–5 の判定に使用。マーカー: `unit` / `integration` / `validation`
- **実ブラウザ検品 `node build/page_check.mjs` も完了条件に含める。** 図が読めるか・
  画面の数字が本文と合っているかはテストで代替できない。検品器は失敗を終了コードで返す
- フィクスチャ更新は専用コミット(`test: update fixtures`)で行い、理由をループログに記す
- 解析解を期待する合成フィクスチャは、期待値の導出前提(直交性・一意性・単一帰属等)を
  **テスト内の assert で検算**し、導出過程をコメントに残す

## 期待値の出所(HC-016)

| 出所 | 書き方 |
|---|---|
| SPEC の条項 | 条項 ID を書く。**SPEC の保証粒度を超える期待値を書かない** |
| 外部権威 | 出典と取得日をフィクスチャの先頭に書く |
| 実測 | **実測日と実測値**をコメントに残す |

件数・行数は定数で書かず、**集合の一致・取りこぼしの不在**という不変量で書く。

**手勘定の期待値も、書いたら必ず一度は実物で数え直す**(HC-002)。小さなフィクスチャは
「読めば分かる」ので確かめずに書きがちだが、読めることと数えたことは別である。
T-010 はこれを踏んで一度落ちた —— 期待した並びは本文の末尾にしか無く、次の字を持っていなかった。

## 主張の向き(HC-001)

**アプリの目玉が「増えるほど良くなる/減る/近づく」という向きを主張するなら、
SPEC にその主張を書く前に、出荷する既定パラメータでその向きを実測する。**
実測が主張と逆を向いたら、まず主張ではなく実装(式・既定値)を疑い、
直せないなら**主張のほうを捨てる**。

本プロジェクトでは実際に捨てた。「文脈は長いほど当たる」は、混合 2 方式 ×
割引 11 点 × 4 コーパスのどれでも成り立たなかった。代わりに成り立った
「谷の位置は読んだ量で決まる」を目玉にし、その向きを T-041 に固定した。

## 定理を使うときの前提(HC-003)

外部の定理・公式を使うときは 3 点セットで置く。

1. **成り立つ条件を式と一緒に書く**
2. **条件を検査する述語を実装に置く**(`is_non_increasing` / `isNonIncreasing`)
3. **条件を外した入力で実際に壊れることを陽性対照として持つ**(T-055)

番人を置くだけでは足りない。条件を外しても何も起きないなら、
その番人は要らないか、番人が見ている条件が違う。

## 検出系テストの陽性対照(HC-002)

**悪い例を原稿に直接書かない。** 書くと、原稿を走査する検査が自分の対照を撃つ。
除外印で逃げると検査に穴が開く。**符号位置から実行時に組み立てる**こと
(`tests/test_hygiene.py` の `_ch`)。検査器が受け取るのは本物の悪い例で、
原稿に残るのは 16 進の数字だけになる。

## オラクルの出所

| フィクスチャ | 出所 | 性格 |
|---|---|---|
| 総当たりの接尾辞配列(`_brute_sa`) | **厳密オラクル**。全接尾辞を素直に並べる参照実装をテスト側に別途書く | 接頭辞倍化の正しさ |
| 小さな本文(`tiny`) | **手勘定**。18 字なので添字ごと数え直せる。数え直した過程をコメントに残す | 出現回数の絶対値 |
| 表 ↔ 配列なめ ↔ 二分探索 | **三経路一致**。同じ量を機構の違う 3 通りで数える | G-01 の土台。同義反復にならない |
| 温度の 2 点閉形式 | **閉形式**。`a^(1/T)/(a^(1/T)+(1-a)^(1/T))` をテスト内で直接評価 | 対数側で引き算する実装が歪めていないこと |
| 一様分布のエントロピー | **閉形式**。`log2 n` | 符号と底 |
| シャノン上下界 | **定理を実測で確認**。エントロピーが厳密に分かる分布を無作為 2,000 個作り、実際に挟むかを見る。**公表値(英文 1.3 bit)は暗記しない** | 式の妥当性 |
| Python 参照実装 ↔ JS 出荷実装 | **二実装照合**。分布・帯・つまみ・乱数・生成文・ビット数のすべて(許容 1e-12) | G-01 |
| 実ブラウザの表示 ↔ 本文の素朴な数え直し | **出力からの読み戻し**。画面に出ている「出現 n 回」を、`indexOf` で数え直して突き合わせる。モデルを呼び直さないので同義反復にならない | G-06 |
| dev 本文 / held 本文 | **場所で分ける**。つまみの選定は dev、報告は held | G-03 |
| 外部の言語モデルライブラリ | **使わない**。平滑化の実装差は正常であり、外部との一致を偽の基準にしない | — |

## ケース一覧

### 接尾辞配列と出荷データ

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-001 | G-02 | 接頭辞倍化 vs 総当たり(6 種の短い本文) | 完全一致 |
| T-002 | G-02 | 出荷済み全コーパス | 置換であり、等間隔標本で隣接接尾辞が昇順 |
| T-003 | N-02 | 出荷済み全コーパス | BMP 外の字が 0 件。UTF-16 符号単位数 = 字数 |

### 数え方

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-010 | F-01 | 18 字の本文で出現回数を手勘定 | 「あい」の直後は う3・あ1。本文末尾で終わる出現は数に入らない |
| T-011 | G-01 | 長さ 1・2 の文脈を表と配列の両方で数える | 一致。**陽性対照**として 1 件ずらせば不一致になることも見る |
| T-012 | G-01 | `distribution()[ch]` と `prob_of(ctx,ch)` | 一致(1e-12)。未出現文脈・空文脈・UNK も含む |
| T-013 | G-01 | 種類数を「なめて」数える道と「跳んで」数える道 | 一致 |

### 確率としての健全性

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-020 | G-04 | 全文脈で分布と寄与の総和 | どちらも 1(1e-9)。負の確率なし |
| T-021 | F-02 | 未知字と、本文に 1 回しか出ない字 | 確率が 0 でない |
| T-022 | F-04 | 長さ m で 0 回のとき | それより長い文脈もすべて 0 回(打ち切りの前提) |

### つまみ

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-030 | F-03 | T=1 | 恒等 |
| T-031 | F-03 | 2 点分布の閉形式 | 一致(1e-9) |
| T-032 | F-03 | T を上げる | エントロピー非減少、かつ端点で厳密に増加 |
| T-033 | F-03 | T→0 / T→大 / 同率 | 最頻値へ集中 / 一様へ / 山分け |
| T-034 | F-03 | top-k | 残るのはちょうど min(k,V) 個、総和 1、集合が上位 k と一致 |
| T-035 | F-03 | top-p | 累積が q に届き、**1 個抜くと届かない**(境界の off-by-one) |
| T-036 | F-03 | top-p の q を上げる | 残る集合は縮まない |
| T-037 | F-06 | 一様・一点のエントロピー | `log2 n` / 0 |
| T-038 | F-06 | 当惑度 | `PPL = 2^H` |
| T-039 | F-06 | 交差エントロピーを外から集計し直す | 一致(1e-9) |

### 文脈の山(目玉)

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-040 | F-07 | 事前計算の走査(`scaling.json`) | 学習量を増やすと曲線全体が下がる(全文脈長で単調) |
| T-041 | F-07 | L=3 と L=2 の差 | 学習量を増やすと縮まり、最大量で符号が反転する。**HC-001 の主張の向きゲート** |

### シャノン上下界

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-050 | F-08 | 無作為 2,000 分布 | 下界 ≤ H ≤ 上界 |
| T-051 | F-08 | 一様分布 | 上下界が一致して `log2 n` |
| T-052 | F-08 | 毎回 1 回目で当たる | 上下界とも 0 |
| T-053 | F-08 | 理想予測器の推測回数 | 確率順の順位と一致 |
| T-054 | F-08 | 理想予測器の q | 上界がエントロピーそのものになる |
| T-055 | G-05 | **前提を外した q** | 前提違反を検出でき、かつ**実際に下界 > 上界になる** |
| T-056 | G-05 | q を並べ替える | 上界は不変(だから人間にも使える) |

### 二実装照合

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-060 | G-01 | 本文の長さ・字種数 | 一致 |
| T-061 | G-01 | 分布上位 12 字 + trace 全段(n・u・γ・寄与) | 一致(1e-12) |
| T-062 | G-01 | **探り文脈の性格** | 「多い」「少ない(≤3)」「無い」がすべて 1 つ以上ある(陽性対照) |
| T-063 | G-01 | 温度 6 点・top-k 3 点・top-p 3 点 | 値も残った集合も一致 |
| T-064 | F-05 | mulberry32 の 16 個 | 一致(1e-15) |
| T-065 | F-05 | 同じたねで 120 字生成 | **1 字も違わない** |
| T-066 | G-01 | 検証本文 2,000 字のビット数 | 一致(1e-12)。OOV 件数も一致 |

### 文字種

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-070 | G-07 | 既知の悪い例 5 種(実行時に符号位置から組み立てる) | すべて捕まえる |
| T-071 | G-07 | 正当な例 5 種(数式のギリシャ文字・タブ・日本語の説明文) | 1 つも撃たない |
| T-072 | G-07 | 原稿すべて | 違反 0 件 |
| T-073 | G-07 | 走査の網 | 主要な原稿 5 本が**名前で**対象に入っている |

### 実ブラウザ検品(`build/page_check.mjs`)

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| B-01 | F-01/F-04 | 起動後の描画 | 棒 ≥10 本・帯 ≥2 区画・内訳 ≥2 行・凡例数 = 帯数 |
| B-02 | G-04 | 帯の幅と表の寄与 | どちらも合計 100%(±0.6) |
| B-03 | G-06 | 表の「出現 n」 | `indexOf` で数え直した回数と一致 |
| B-04 | F-03 | top-p を 0.5 に絞る | 残る候補が実際に減る |
| B-05 | F-05 | 「次の一字を書く」 | 本文がちょうど 1 字伸びる |
| B-06 | フリート規約 | フッタ | リンク 5 本・著作権表示あり・`position: fixed` |
| B-07 | — | 全タブ | 開ける。コンソールエラー 0 件 |
