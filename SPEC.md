# SPEC.md — tsugi-no-hito-ji(次の一字)

<!-- scaffold template v1.13.0 から展開(2026-08-29)。以後このファイルはプロジェクトが育てる -->

## 1. 目的

大規模言語モデルの「出口」——**次の一トークンの確率分布と、そこからどう選ぶか**——を、
実際に動く小さな言語モデルの上で最後まで見せる。温度・top-k・top-p が何をしているのか、
文脈が長いと何が変わるのか、そして**人間とモデルはどちらがよく当てるのか**を、
すべて実測値で答える。

扱う単位は**文字**である。トークン分割器を持ち込むと、分割の是非という別の論点が入り、
しかも分割器の中身がブラックボックスになる。文字なら数え方に一点の曖昧さもなく、
シャノンが 1951 年に英文でやった実験(彼も文字単位だった)をそのまま日本語に移せる。

**当初の目玉は「文脈は長いほど当たる」だった。実測がそれを否定したので捨てた**(§10)。
いまの目玉は「**効く文脈の長さは、読んだ量が決めている**」である。

## 2. データ

| 項目 | 値 |
|---|---|
| 出典 | 青空文庫(aozora-sakuin の正規化済み本文 5,000 作から採録) |
| 採録条件 | 新字新仮名のみ。旧仮名を混ぜると「人間 対 モデル」が仮名遣いの勝負になる |
| 整形 | ルビ・注記・見出し・底本表記は除去済み。**表記は書き換えない**(NFKC もかけない)。改行は残す |
| 除去 | BMP 外の文字(JS と Python で添字を一致させるため)、3 連以上の改行 |
| 採録日 | 2026-08-29 |

### 出荷しているコーパス(実測・2026-08-29)

| id | 本 | 字数 | 字種 | 作品数 | 調整用(dev) | 報告用(held) |
|---|---|---:|---:|---:|---|---|
| `soseki` | 夏目漱石 | 400,000 | 3,138 | 12 | 『吾輩は猫である』上篇自序 | 『それから』 |
| `dazai` | 太宰治 | 343,816 | 2,578 | 12 | 『川端康成へ』 | 『ヴィヨンの妻』 |
| `kenji` | 宮沢賢治 | 169,664 | 1,539 | 9 | 『注文の多い料理店』 | 『やまなし』 |
| `mixed` | 近代小説つめあわせ | 399,991 | 2,706 | 15 | 『源氏物語』 | 『痴人の愛』 |

各コーパスから**2 作を抜いてある**。1 作(dev)は割引 D を選ぶためだけに、
もう 1 作(held)は数字を報告するためだけに使う。同じ本文で選んで同じ本文で報告すると
較正が循環する(G-03)。

### 出荷物

モデルファイルは配らない。**本文そのものと接尾辞配列**を配り、確率はその場で数える。

| ファイル | 中身 |
|---|---|
| `web/data/<id>.txt` | 本文(UTF-8) |
| `web/data/<id>.sa.bin` | 接尾辞配列(Int32 リトルエンディアン、要素数 = 文字数) |
| `web/data/<id>.held.txt` / `.dev.txt` | 報告用・調整用の本文(各最大 20,000 字) |
| `web/data/corpora.json` / `scaling.json` / `baseline.json` | 目録と事前計算値 |

初回表示で読むのは既定コーパス 1 つだけ(最大 2.8MB)。

## 3. 確率の作り方

長さ 0(単字)から長さ L(既定 8)までの文脈を、**絶対割引で下から混ぜ上げる**。

```
p_0(c) = (N_c + 1) / (N + V + 1)                       … 加算 1。未知字を 1 型として含む
p_m(c) = max(k_m(c) - D, 0) / n_m  +  γ_m · p_{m-1}(c)
γ_m    = D · u_m / n_m
```

- `n_m` = 長さ m の文脈が出現し、かつ次の字を持っていた回数
- `u_m` = その直後に来た字の種類数
- `k_m(c)` = 長さ m の文脈の直後に c が来た回数
- `D` = 0.95(**dev 本文の上だけで選定** — §5)

見た回数から D を引き、引いた分をまとめて短い文脈に譲る。1 回しか出ていない文脈は
γ = 0.95、つまり 20 分の 19 を短い文脈に譲る。`0 < D < 1` かつ `k ≥ 1` なので
`max(·,0)` は常に正で、`Σ_c p_m(c) = 1` が場合分けなしに成り立つ。

長さ m で 0 回なら、それより長い文脈も必ず 0 回なので、そこで打ち切ってよい。

### 数え方(同じ量に 3 通り)

| 経路 | 使う場所 | 機構 |
|---|---|---|
| 走査して作る表 | 長さ 1・2 | 本文を 1 回なめて `Map` に積む |
| 接尾辞配列の範囲をなめる | `next_counts` / 長さ 3 以上 | 二分探索で範囲を出し、範囲内を数える |
| 二分探索だけ | `prob_of` / `distinct_followers` | 範囲をなめずに、出現数と種類数を跳び歩きで取る |

3 つが一致することを T-011 / T-012 / T-013 で確かめる。
絶対値は、紙で数え直せる小さな本文(T-010)と総当たり(T-001)で固定する。

## 4. 分布の加工(出口のつまみ)

| つまみ | 式 | 満たすべき性質 |
|---|---|---|
| 温度 T | `p_i ∝ p_i^(1/T)` | T=1 は恒等。T について**エントロピーは非減少**。T→0 で最頻値(同率は山分け)、T→∞ で一様 |
| top-k | 上位 k 個を残して正規化 | 残るのはちょうど min(k, V) 個。k≤0 は制限なし |
| top-p | 累積が q を**初めて超えるところまで**残して正規化 | 必ず 1 個以上残る。q を上げると集合は縮まない |

並べ方は「確率の降順、同率は字の昇順」。決めておかないと両実装で結果がずれる。
乱数は mulberry32、抽出は字の昇順に並べた上での逆関数法。

## 5. 較正(G-03 循環の禁止)

割引 D は `build/calibrate.py` が **dev 本文の上だけ**で選ぶ。0.1〜0.99 の 11 点を掃き、
4 コーパスの平均ビット数が最小の値を採る。コーパスごとに別の D を選ぶことはしない
(コーパスを替えたときの数字の動きが、モデルの性質なのか調整の結果なのか分からなくなる)。

**実測(dev・2026-08-29)**: 平均 6.1222 bits で D = 0.95 を選定。
D=0.9 が 6.1279、D=0.75 が 6.2644、D=0.5 が 6.7251。

### 報告値(held・D=0.95・2026-08-29)

L は文脈の上限。太字が最良。

| 本 | L=0 | L=1 | L=2 | L=3 | L=4 | L=8 |
|---|---:|---:|---:|---:|---:|---:|
| 夏目漱石 | 7.861 | 5.872 | 5.437 | **5.425** | 5.472 | 5.523 |
| 太宰治 | 7.174 | 5.327 | 4.701 | **4.670** | 4.737 | 4.816 |
| 宮沢賢治 | 7.506 | 6.316 | **5.951** | 6.028 | 6.145 | 6.323 |
| つめあわせ | 7.833 | 6.269 | **5.969** | 6.028 | 6.098 | 6.167 |

## 6. 機能要求

| ID | 要求 | 優先度 |
|---|---|---|
| F-01 | 次の一字の確率分布を、上位 16 字まで棒で見せる。切り落とされた候補は薄く残す | must |
| F-02 | 見たことのない字にも 0 でない確率を残す(交差エントロピーを有限に保つ) | must |
| F-03 | 温度・top-k・top-p・文脈上限をつまみで動かし、分布とエントロピーの変化を即時に見せる | must |
| F-04 | **どの長さの文脈が最終確率の何割を持っているか**を帯と表で見せる。帯・表・文はすべて同じ trace から作る | must |
| F-05 | 同じたね・同じつまみなら、生成される文章が 1 字も違わない | must |
| F-06 | 文脈長ごとのビット数を**その場で測り**、谷の位置を示す | must |
| F-07 | **読んだ量を変えた曲線の束**を見せ、谷が右へ動くことを示す(事前計算) | must |
| F-08 | かなあて勝負。人間とモデルが同じ位置で当てっこし、推測回数からビット数を挟む | must |
| F-09 | コーパスを 4 つから選べる。替えると同じ書き出しでも次の字が変わる | should |
| F-10 | 測って捨てた主張(「文脈は長いほど当たる」)を、たねあかしに残す | should |

## 7. 非機能要求

| ID | 要求 | 検証方法 |
|---|---|---|
| N-01 | 出荷物は静的ファイルのみ。実行時の外部通信ゼロ・API 鍵ゼロ・課金ゼロ | 依存は検品用の playwright のみ。`web/` に fetch 先は自分の `data/` しかない |
| N-02 | Python と JS の文字添字が一致する | T-003(BMP 外の字が残っていない・符号単位数 = 字数) |
| N-03 | 原稿にキリル文字・許可外のギリシャ文字・制御文字が混入しない | T-070〜T-073(`build/hygiene.py`) |
| N-04 | 初回表示で読むデータは 3MB 以下 | 既定コーパス soseki = 本文 1.19MB + 配列 1.60MB |
| N-05 | スマホの幅(360px)で横スクロールが出ない | 実ブラウザ検品 |

## 8. 品質基準(ゲート)

| ID | 基準 | 落ちたら |
|---|---|---|
| G-01 | 参照実装(Python)と出荷実装(JS)が、分布・帯・つまみ・乱数・生成文・ビット数のすべてで一致する(許容 1e-12) | 出荷しない |
| G-02 | 接尾辞配列が置換であり、隣接接尾辞が昇順である(全コーパス) | 配らない |
| G-03 | つまみの選定は dev 本文、報告は held 本文。**同じ本文で選んで報告しない** | 数字を出さない |
| G-04 | 分布の総和が 1、帯の寄与の総和が 1 | 出荷しない |
| G-05 | 定理を使うときは、成り立つ条件を述語で検査し、**条件を外すと実際に壊れる**ことを対照で示す | 出荷しない |
| G-06 | 画面の「出現 n 回」が、本文を素朴に数え直した回数と一致する(実ブラウザで検算) | 出荷しない |
| G-07 | 原稿の文字種検査に違反 0 件 | 出荷しない |

## 9. スコープ外

- **単語・トークン単位の言語モデル。** 分割器の是非という別の論点が入る(分割そのものは姉妹アプリ `token-hakari` の主題)
- **ニューラル言語モデル。** 学習済み重みを配ると数百 MB になり、しかも「その場で数えた」と言えなくなる
- **外部 API の呼び出し。** 鍵が要るものは出荷しない
- **本物の GPT / Claude のトークナイザとの比較。** 語彙表の同梱はライセンスと外部依存を招く
- **文章生成の品質を上げること。** 40 万字の文字 n-gram に文章は書けない。書けないことが見えるほうが値打ちがある

## 10. 測って捨てたもの

| 捨てた主張 | 何を測ったか | 結果 |
|---|---|---|
| 「文脈は長いほど次の字が当たる」 | 混合 2 方式(λ=n/(n+β) / 絶対割引)× 割引 11 点 × 文脈長 0〜8 × 4 コーパス | **どの組み合わせでも 4 文字を超えると悪化**。主張を捨て、「谷の位置は読んだ量で決まる」に置き換えた(HC-001) |
| 人間のエントロピー下界 | かなあて勝負の実測 | 下界の式は「確率の降順で当てにいく」ことが前提。人間は満たさず、**下界が上界を上回った**。人間には上界だけを出す(HC-003) |
